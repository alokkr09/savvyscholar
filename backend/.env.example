# Server Configuration
PORT=5001
NODE_ENV=development

# Database Connection (MongoDB URI)
# For local MongoDB: mongodb://127.0.0.1:27017/savvyscholar
# In development, if unconnectable, an in-memory Mongo server instance will automatically spin up.
MONGODB_URI=mongodb://127.0.0.1:27017/savvyscholar

# Authentication & JWT
JWT_SECRET=savvy_scholar_super_secure_jwt_secret_dev_2026_key_#123
JWT_EXPIRES_IN=7d

# Client / CORS
CLIENT_URL=http://localhost:5173
