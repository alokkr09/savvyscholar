{
  "name": "savvy-scholar",
  "version": "1.0.0",
  "description": "Production-grade personal finance management platform designed for students and young adults.",
  "private": true,
  "scripts": {
    "dev": "concurrently -n \"BACKEND,FRONTEND\" -c \"blue,green\" \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend",
    "build": "npm --prefix backend install --include=dev && npm --prefix backend run build && npm --prefix frontend install --include=dev && npm --prefix frontend run build",
    "build:backend": "npm run build --prefix backend",
    "build:frontend": "npm run build --prefix frontend",
    "start": "node backend/dist/server.js",
    "test": "npm run test --prefix backend",
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "keywords": [
    "fintech",
    "personal-finance",
    "student-budgeting",
    "react",
    "express",
    "mongodb",
    "typescript"
  ],
  "author": "Savvy Scholar Team",
  "license": "MIT"
}
